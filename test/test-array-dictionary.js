// Generated by CoffeeScript 1.7.1
(function() {
  describe('ArrayDictionary', function() {
    describe('set', function() {
      var arrayDict;
      arrayDict = new ArrayDictionary;
      it('should be able to set an array to an object', function() {
        return (function() {
          return arrayDict.set([1, 2], {
            a: 1
          });
        }).should.not["throw"]();
      });
      return it('should throw on a not-array key', function() {
        return (function() {
          return arrayDict.set('someKey', {});
        }).should["throw"]();
      });
    });
    describe('get', function() {
      return it('should retrieve previously-stored value', function() {
        var arrayDict, object;
        object = {
          a: 1
        };
        arrayDict = new ArrayDictionary;
        arrayDict.set([1, 2], object);
        return arrayDict.get([1, 2]).should.equal(object);
      });
    });
    return describe('keys', function() {
      var arrayDict;
      arrayDict = new ArrayDictionary;
      it('should return an empty array when empty', function() {
        return arrayDict.keys().should.be.empty;
      });
      return it('should return a key that is set', function() {
        arrayDict.set([1, 2], {});
        return arrayDict.keys().should.eql([[1, 2]]);
      });
    });
  });

}).call(this);
